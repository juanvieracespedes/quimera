import React from "react";

const NavBarStyle = () => {
  const styles = {
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    padding: "0px 5px",
    width: "100%",
    backgroundColor: "beige",
    position: "sticky",
    top: '0',
  };

  return styles;
};

export default NavBarStyle;